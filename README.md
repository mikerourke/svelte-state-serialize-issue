# svelte-state-serialize-issue

Minimal reproducible example for Svelte state serialization issues.

## Instructions

- Run `npm install`
- Run `npm start`
- Click the buttons in the UI, errors will log out in the console.
